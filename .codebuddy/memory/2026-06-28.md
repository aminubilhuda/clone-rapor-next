# 2026-06-28

## Chrome DevTools MCP & Browser Testing
- chrome-devtools MCP digunakan untuk mengoperasikan browser
- Berhasil navigasi, screenshot, dan snapshot halaman TU

## Analisis Menu TU
- Semua fitur menu TU diidentifikasi: Dashboard, Profile Sekolah, Pegawai, Data Siswa, Mapel, Ekstra, Kompetensi, Prakerin, Deskripsi Rapor, Kelas/Rombel, Anggota Kelas, Mapel Kelas, Mapel Pilihan Siswa, Naik Kelas, Laporan Pendidikan, P5BK, Piket Harian, Pengaturan

## Modifikasi Menu P5BK - Bagian 1: Kolom Tabel
- Kolom ID dan Kode dihapus dari tabel P5BK
- Kolom No (nomor urut) ditambahkan
- File: `p5bk-client.tsx`

## Modifikasi Menu P5BK - Bagian 2: Edit Modal dengan Sub Elemen
- Modal edit P5BK diubah: menampilkan Pengguna/Pembina, Judul Proyek, Deskripsi Proyek, dan tabel ceklis Dimensi/Elemen/Sub Elemen
- Data diambil dari tabel: `dimensi`, `elemen`, `sub_elemen`, `proyek_subelemen`
- Server action `getSubelemenByProyek` ditambahkan untuk fetch sub_elemen terpilih
- `updateP5BK` dimodifikasi untuk save sub_elemen (delete existing + insert baru)
- File yang dimodifikasi: `page.tsx`, `p5bk-actions.ts`, `p5bk-client.tsx`, `modal-p5bk.tsx`

## Modifikasi Menu P5BK - Bagian 3: Tombol & Modal Nilai
- Tombol Nilai (icon chart biru) ditambahkan di kolom Aksi tabel P5BK
- Modal Nilai P5BK menampilkan: Nama Siswa | Dimensi (dropdown: MB/SB/BSH/SAB) | Cetak Rapor | Deskripsi/Catatan
- Tabel siswa diambil dari `siswa_kelas` JOIN `siswa` (kelas sesuai proyek)
- Dimensi dinamis dari `proyek_subelemen` JOIN `dimensi`
- Server action `getDataNilaiP5BK`: fetch siswa, dimensi, existing nilai
- Server action `saveNilaiP5BK`: upsert nilai_kokurikuler + auto-create proyek_tujuan per (proyek, dimensi)
- File baru: `modal-nilai-p5bk.tsx`
- File dimodifikasi: `p5bk-actions.ts`, `p5bk-client.tsx`

## Modifikasi Menu P5BK - Bagian 4: Fix Bug + Perpindahan ke Tabel `nilai_proyek`
- Fix `unknown column d.kode_dimensi` → kolom tidak ada di tabel `dimensi`
- Fix duplicate key error di `p5bk-client.tsx` → `<td key={col.key}>` dirubah ke `<td key={${row.id_proyek_kelas}-${col.key}}>`
- Fix duplicate key error di `modal-nilai-p5bk.tsx` → query siswa pakai `SELECT DISTINCT`
- Tabel `nilai_kokurikuler` SALAH untuk P5BK →改成 ke `nilai_proyek` (sudah ada data di DB)
- Filter tahun/semester diterapkan: siswa & nilai difilter berdasarkan `proyek.tahun` dan `proyek.semester` dari `proyek_kelas`, DAN `nilai_proyek.tahun/semester`
- Struktur `nilai_proyek`: `tahun`, `semester`, `proyek`, `id_kelas`, `id_mapel`, `id_dimensi`, `id_elemen`, `id_sub_elemen`, `id_siswa`, `nilai`
- Modal redesigned: header 2 baris (dimensi colspan + sub_elemen names), tanpa kolom catatan
- `getDataNilaiP5BK` sekarang JOIN `proyek_subelemen` → `dimensi` → `elemen` → `sub_elemen` + query `nilai_proyek`
- `saveNilaiP5BK` sekarang INSERT/UPDATE ke `nilai_proyek` per sub_elemen (bukan per dimensi)
- Data existing di modal (dropdown pre-selected SAB/BSH/MB/SB) ✅ sudah berfungsi
